import os
os.chdir(r"A:\Data science")
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import cv
import math
import PIL
import IPython
import cv2
from IPython.display import display
from PIL import Image
import re
#with open("d.txt","r") as file:
      #wiki=file.read()
#for title in re.findall("[(]\d{3}[)]\s\d{3}[-]\d{4}",wiki):
      #print(title)

#a="""Amy is 5 years old, and her sister Mary is 2 years old.
    #Ruth and Peter, their parents, have 3 kids."""
#print(a)
#lst=list()
#for b in re.findall("[Amy]{3}|[Mary]{4}|[Ruth]{4}|[Peter]{5}",a):
    #print(b)
    #lst.append(b)
#lst=list()
#with open ("grades.txt", "r") as file:
        #grades = file.read()
#print(grades)
#for word in re.findall(".*[:][\s][B$]",grades):
    #a=word.split(":")
    #print(a[0])
    #lst.append(a[0])
    #print(word)
#import re
#with open("logdata.txt","r") as file:
   #wiki=file.read()
#print(wiki)
#logdict=dict()
#pattern = """"
  #(?P<host>.*)
  #(\ -\ )
  #(?P<username>.*)
  #(\ \[)
  #(?P<time>.*)
  #(\]\ \")
  #(?P<request>.*)
  #(\")"""
#for item in re.finditer(pattern,wiki,re.VERBOSE):
    #logdict.append(item.groupdict())
    #print(logdict)


# to convert lists to dictionary
# res = dict(zip(test_keys, test_values))
#for i in re.findall():



